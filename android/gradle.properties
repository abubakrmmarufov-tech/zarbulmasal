org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# Reject Android/Gradle artifacts that are not covered by verification metadata.
org.gradle.dependency.verification=strict
android.useAndroidX=true
# Flutter's current Gradle plugin still uses the legacy APIs, so keep these
# compatibility switches until the plugin supports AGP 9's defaults.
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
