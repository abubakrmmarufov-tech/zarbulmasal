## 2025-02-15 - Initial Audit\n**Learning:** The prompt implies fixing multiple bugs, including Spaced Repetition (flashcards) not persisting, daily content logic not working correctly (based on time difference instead of exact date logic causing offset across year boundaries maybe, or simple year*365 logic), duplicates in authors, some works might reference invalid authorIds if deduplication wasn't careful, etc.\n**Action:** Plan to go through them one by one.
## Analysis\n- Flashcards mastery logic is entirely missing (it only shows a Snackbar). Needs to be implemented with an actual state notifier or similar that saves to SharedPreferences (or a DB) and impacts scheduling.\n- Daily verse logic in `lib/features/literature/data/literature_repository.dart` uses a simplistic  which has year-boundary and leap-year bugs. It should be refactored to use  from .\n- Author deduplication: `Абуалӣ ибни Сино` vs `АБУАЛӢ ИБНИ СИНО` and `Боботоҳири Урён` vs `БОБОТОҲИРИ УРЁН`. Needs a careful merge, updating works that point to the duplicate ID.
