# QA Issue Register

Current snapshot: branch `provenance-repair-2026-09-19` at audited source checkpoint `49f54cf`. The same app/data/assets/tests are published on GitHub branch `audit-publish-2026-09-22` at `a28d89d`; its workflow file intentionally remains unchanged because the connected OAuth credential lacks the `workflow` scope. The current web-only Pages deployment is `gh-pages` commit `3020730` with cache `75792ceb8692e786ace2`; the former ignored v1.1.0 APK set has been moved to a recoverable temporary quarantine outside the checkout; neither is evidence for the current source release. No public-download APKs remain in the repository checkout.

Status values: OPEN, PARTIAL, FIXED/VERIFIED, UNVERIFIED.

Current literary evidence override (2026-09-22): the validator now reports
**31 primary-checked works, 25 secondary-witness collations, and 6 primary
records still lacking a second witness**. The latest loop added visually
checked textbook witnesses for additional classical and modern works, corrected
one Bedil/Tughrol attribution, and quarantined duplicate/prose fragments. The
newly collated witness is
Kamoli Khujandi’s complete eight-line ghazal «Дӯст медорад дилам ҷавру ҷафои
дӯстро» (current Maorif Grade 7, 2025, pp. 102–103), exact after visual
comparison; rights remain unknown and full text remains withheld. The detailed
rows below retain their earlier narrative history where applicable.
The current loop also collated Sayyido’s nine-line «ОМАД БА ЁД» between Grade
10 p. 74 and the independent Grade 7 p. 161 witness; the spelling variant is
recorded and the page proof is retained.
The current source audit also removed a cross-linked 2025 witness from the
distinct «Ошӯби ҷонӣ» record and retained it only on the matching «Дӯст медорад
дилам ҷавру ҷафои дӯстро» record; both remain review-only.
The same sweep added a visually inspected six-line Hiloli occurrence from the
uploaded Grade 9 p. 316 page; it is recorded as `sourceOccurrences` only and
does not count as a complete second witness.
The pending audit also currently reports 5,217 records still missing the
line/script audit gates and 0 missing printed primary pages; none are
displayable.

The 2026-09-22 update added image-backed official Grade 7 2025 witnesses for
Firdausi (printed p. 46, minor typographic variant) and Kamol (printed p. 272,
exact), plus an exact official Grade 11 2025 witness for
Лоиқ Шералӣ’s «Шоири фарзонаро асру замон» on printed page 288. The current
authoritative counts are therefore 25 secondary collations and 6 remaining
primary-checked records without a second witness.

The current validator supersedes those earlier pending counts with **5,217
needs-review works, 253 rejected extraction/prose or duplicate candidates, and
0 pending review records without a printed page**. Rights remain unknown and all
page-checked works remain withheld from publication.

The detailed issue rows below may retain historical numbers; this paragraph is
the authoritative current snapshot for the active worktree.
The latest full suite remains **396/396**, with **82.19% line coverage
(7,736/9,412)**, clean analysis, and a fresh current-data browser sweep at 8
viewports and 209 routes with zero errors.

| ID | Severity | Status | Area | Reproduction / evidence | Root cause | Required protection |
| --- | --- | --- | --- | --- | --- | --- |
| LIT-001 | P0 | FIXED/VERIFIED | Literary publication gate | Open Literature → Works. The app shows a pending-review state rather than exposing the 5,501 imported/candidate records. `approvedWorksProvider` returns only displayable records. | Imported records now use the domain schema and remain `needsReview` until all verification gates pass. | JSON validation and content validation in CI; retain fail-closed `isDisplayable`. |
| LIT-002 | P1 | OPEN | Literary provenance truthfulness | **Current authoritative counts:** 5,217 of the 5,501 work records remain pending; 253 high-confidence classroom-question, explanatory-prose, or duplicate extraction false positives are explicitly rejected; 31 records are primary-page-checked, 25 have secondary-witness collations, and 6 still lack a second witness. All 31 remain rights-unknown and non-displayable. The detailed witness history below is retained as an audit trail. Thirty-one same-author extraction duplicates were merged into canonical records with 38 retained source occurrences; additional same-author duplicate candidates are explicitly linked to their canonical records and quarantined. Sayyido’s nine-line «ОМАД БА ЁД» is now collated between Grade 10 p. 74 and independent Grade 7 p. 161 witnesses with a recorded minor spelling variant and local page proof. Four exact Ministry-hosted witnesses were additionally collated for Bedil p. 146, Hafez p. 93, Lоҳутӣ p. 113, and Loiq p. 288; all remain review-only. Firdausi’s two-line maxim now has a Grade 7 2018 p. 47 / current Maorif Grade 7 2025 p. 46 collation with a minor typographic variant, and Kamol’s two-line couplet has an exact Grade 7 p. 125 / current Maorif Grade 7 2025 p. 272 collation; both retain local page-image proof. Kamoli Khujandi’s eight-line ghazal now has an exact Grade 7 2018 p. 106–107 / current Maorif Grade 7 2025 pp. 102–103 collation with local page-image proof. Rudaki’s four-line record now has an exact Grade 5 p. 50 / Grade 6 p. 12 collation, and the 12-line «Бӯйи Ҷӯйи Мулиён» record now has a complete Ministry Grade 5 p. 56 secondary witness with a minor orthographic variation from its Grade 5 p. 54 primary. Saadi’s six-line record has an exact Grade 5 p. 111 / Grade 9 p. 39 collation, and Ibn Sina’s sixteen-line record has an exact Grade 5 p. 71 / Grade 8 p. 135 collation; the corrected Ibn Sina rubai now also has an exact Grade 5 p. 62 / Grade 8 p. 142 collation. Rabi’a Balkhi’s sixteen-line qasida now has an exact Grade 5 p. 60 / Ministry-hosted 2022 Grade 5 pp. 61–62 collation with local page-image proof. Sadriddin Ayni’s four-line poem now has a complete Grade 5 p. 153 / Ministry-hosted 2022 Grade 5 p. 132 collation with a minor orthographic variant (`бекориву` / `бекорию`) and local page-image proof. Hafez’s 16-line ghazal now has an exact Grade 9 p. 170 / Ministry-hosted 2023 Grade 9 p. 173 collation; its Grade 10 p. 201 opening-couplet occurrence remains recorded. Tursunzoda’s four-line excerpt now has an exact Grade 11 p. 161 / Ministry-hosted 2022 Grade 11 p. 160 collation, and Bozor Sobir’s eight-line excerpt now has an exact Grade 11 p. 313 / Ministry-hosted 2022 Grade 11 p. 313 collation. Qanoat’s «Мавҷ дар саҳро» now has a Grade 6 pp. 147–148 / Ministry-hosted 2022 Grade 6 p. 148 collation with minor punctuation/orthographic variants; «Мавҷи одам» and «Мавҷи бародарӣ» now also have exact Ministry Grade 6 second witnesses on pp. 149–150 and 150–151, respectively. A fresh read-only maorif.tj/PDF sweep added exact Ministry textbook page leads for Фарҳат (printed p. 61), Бадри Чочӣ (printed p. 8), Александр Пушкин (pp. 11, 119), Михаил Лермонтов (p. 275), Самуил Маршак (p. 119), Виктор Гюго (pp. 12, 119), and Рафаэл Патканян (p. 119); these records remain review-only because the pages do not establish complete identity/biography/portrait evidence. All remain non-displayable because rights are unknown and editorial approval is absent. Runtime assets contain no full-text fields or incipits for rights-unknown works, and the 14 rights-unknown oral records contain metadata only. Poet profiles now list only pending records with both a printed page and source reference; unlinked extraction leads remain in the audit dataset and are counted but not presented as source-backed work titles. | Extraction output was previously being treated as bibliographic evidence and had also allowed candidate text/excerpts into active JSON. | Require non-empty, source-backed edition fields and real page ranges before approval; keep candidate text and excerpts in the review workspace only, and do not fabricate missing values. |
| LIT-003 | P1 | FIXED/VERIFIED | Content validation | The validators now agree: 0 editorially approved, 31 primary-page-checked, 253 explicitly rejected false positives or duplicate extractions, and 5,217 pending. The content validator now explicitly reports `PRIMARY CHECKED MISSING SECOND SOURCE: 6`; 25 completed secondary-witness collations are recorded, including Firdausi p. 46, Kamol p. 272, Sayyido p. 161, and exact official witnesses for Bedil p. 146, Hafez p. 93, Lоҳутӣ p. 113, and Loiq p. 288. Pending counts explicitly cover missing second source, page, line, and script audits; none are displayable. The strict provenance linter requires verified local page imagery and complete bibliographic identity for checked and secondary witnesses. | Validation summary previously focused on approved records and hid pending deficiencies; image evidence was previously matched by filename rather than by the declared local path, and checked citations could omit bibliographic identity or visual page proof. | Keep validator and source-register synchronization in CI and require explicit review before promotion. |
| LIT-005 | P0 | FIXED/VERIFIED | False literary approval | A newly modified 82-work batch displayed full texts and claimed page-level verification although every affected primary source omitted `pageStart`. | Approval relied on self-reported checks instead of requiring the page evidence it claimed to have reviewed. | Approved works must now include a primary-source page and `pageVerified`; the batch is restored to `needsReview` and full-text publication is disabled. |
| LIT-006 | P1 | FIXED/VERIFIED | School-canon edition drift and opaque citation state | Grade 5–11 canon cards cited stale or mismatched editions (for example Grade 5 showed 2018 despite the held 2017 Маориф PDF) and exposed no citation-review state. The seven held textbook editions are now registered with exact imprint metadata; each Grade 5–11 mapping links to its source ID and presents its evidence as pending until page review. | Imported curriculum mappings and local PDFs were never reconciled edition-by-edition. | JSON regression requires every Grade 5–11 mapping to reference a held official-textbook source with matching publisher, year, and author line. The UI regression and fresh release-browser flow verify the visible pending disclosure. |
| LIT-007 | P1 | PARTIAL | Textbook corpus is not yet fully verified | Directly inspected Grade 5 witnesses now support Rudaki’s year-only biography on printed page 49, a six-bayt excerpt on printed page 54, and the four-line Rudaki record on printed page 50; the latter now has an exact secondary witness on Grade 6 printed page 12. Rudaki’s 12-line «Бӯйи Ҷӯйи Мулиён» record now also has a complete Ministry Grade 5 2025 witness on printed page 56 with a minor orthographic variant. Saadi’s six-line record is now exactly collated between Grade 5 printed page 111 and Grade 9 printed page 39. Ibn Sina’s sixteen-line record is now exactly collated between Grade 5 printed page 71 and Grade 8 printed page 135, and the four-line “Имрӯз бикун чу метавонӣ коре” rubai is now exactly collated between Grade 5 p. 62 and Grade 8 p. 142 after the prior Rabi’a link was disproved by visual page review. Rabi’a Balkhi’s sixteen-line qasida is now exactly collated between Grade 5 p. 60 and the Ministry-hosted 2022 Grade 5 pp. 61–62. Grade 5 also supports a four-line Tursunzoda excerpt from «Модар» on printed page 216. Grade 7 pages 105–107 now support the recorded Kamoli Khujandi page witnesses, including the 106–107 continuation. The prior Kamol «Гуфтам ба чашм» source-image mismatch is corrected: Grade 7 p. 105 is primary and Grade 9 p. 197 is recorded as a secondary witness, with the observed variants documented. The current loop adds three page-checked Qanoat poem witnesses spanning Grade 6 pp. 147–151 and Sayyido’s nine-line «ОМАД БА ЁД» witness on Grade 7 p. 161. The records are deliberately `primaryChecked`, rejected, or `needsReview`; 5,217 pending works and 253 explicit false positives or duplicate extractions remain in the audit trail, while the other textbook-page candidates still lack the editorial checks required for publication. The current loop also extracted 67 authentic textbook portraits and removed seven duplicate author entities, including a Saadi record carrying Ubayd Zokoni’s biography plus the later Sanai, Jami, and Shahid Balkhi semantic clones; source rights remain unknown and are explicitly not claimed as cleared. | PDF extraction and page indexing can locate candidates but cannot establish complete inclusion, exact text, authorship, composition dates, poem counts, or publication rights. | Process candidate page hits one witness at a time, record printed-page citations, add a second witness, collate line by line, and only then promote records through the strict approval gate. Keep unknown dates/counts absent rather than inferred. |
| LIT-008 | P2 | FIXED/VERIFIED | Pending work links were misleading | A direct link to a known `needsReview` textbook record previously rendered the generic “work not found” state. It now identifies the record as under review and shows its recorded citation, while withholding poem text; unknown and rejected IDs remain unavailable. | The reader only queried the approved-work provider, so it could not distinguish a quarantined record from an invalid ID. | Provider/presentation regressions cover Tajik and Persian pending states; fresh release-browser verification confirms the page-54 citation and zero browser diagnostics. |
| LIT-009 | P2 | FIXED/VERIFIED | Opaque biography citations looked authoritative | 161 author records used only the label “Маҷмӯаи мактабӣ,” without a printed page. Poet profiles now distinguish page-bearing citations from import labels and explicitly disclose when an auditable page source is missing. | The author model treated any non-empty source string as an authoritative citation. | Domain and presentation tests require page-citation detection; keep page-level evidence mandatory before describing imported biography facts as textbook-verified. |
| LIT-010 | P2 | FIXED/VERIFIED | Pending works were undiscoverable from poet profiles | Profiles showed `0` works when all records were quarantined, even though known candidate titles existed. Profiles now separate approved work counts from pending review records, show page citations only when recorded, label missing page numbers, and route review cards to the protected pending reader. | The profile consumed only `approvedWorksProvider` and had no review-only author index. | Keep approved and review providers separate; test Tajik/Persian labels, citation/no-page disclosure, and pending-card navigation without exposing text. |
| LIT-014 | P2 | FIXED/VERIFIED | Secondary/source-occurrence citations were incomplete in the reader | The SourcePanel now renders each secondary witness and every additional source occurrence with its complete human-readable bibliographic citation, including exact printed pages, instead of exposing only the book and publisher metadata. A focused regression covers both roles, and the built web panel was visually checked in dark mode and Persian RTL. | The panel displayed bibliographic fields for additional witnesses but omitted the canonical citation string. | Keep the citation regression and verify long citations in mobile, dark, and RTL layouts whenever provenance presentation changes. |
| LIT-011 | P2 | FIXED/VERIFIED | Persian biography fallback was implicit | In Persian mode, the page-cited Loïc Sherali biography is available only in Tajik Cyrillic. The profile now explicitly says that the biography is being shown in Tajik Cyrillic and retains the page-254 citation; it does not silently present the script fallback as Persian translation. | Biography provenance and script availability were rendered independently, without a user-facing fallback disclosure. | Keep the Persian widget regression and fresh release-browser check; add a Persian translation only when it is separately verified. |
| LIT-012 | P1 | FIXED/VERIFIED | Duplicate Jami dates conflicted with the textbook | Two duplicate Jami author records reported 18 August 1414 and 19 November 1492 with the opaque source label “Маҷмӯаи мактабӣ”; the inspected Grade 7 page 109 says 7 November 1414 and 9 November 1492. Both records now agree on the page-cited dates, birthplace, biography, and source. | Imported author records were accepted without reconciling duplicate IDs against the held textbook witness. | Keep duplicate-author consistency tests and require a printed-page citation before exact dates are user-visible. |
| LIT-013 | P1 | FIXED/VERIFIED | Source-register evidence inflation | The source register now labels unsupported canonical editions as legacy/unverified bibliographic claims, marks institutional rows as unverified leads or edition-match requirements, and states the checked-witness boundary. A regression test prevents the old `CONSULTED` and `ACTIVE CORE REPOSITORY` language from returning. | Historical planning metadata was written as if it were current witness evidence. | Keep the source-register regression test and never promote a work from bibliography-only metadata; current release provenance remains governed by page-proof records, active `sourceReference` fields, and the strict linter. |
| UX-001 | P1 | FIXED/VERIFIED | Literature navigation | Local runtime and widget tests show School Canon and Search are reachable from the hub; Oral Heritage is visibly unavailable when empty and cannot be tapped. | Hub entries now reflect available data and route directly to the implemented screens. | Keep route/action coverage in widget tests whenever a hub entry changes. |
| UX-002 | P1 | FIXED/VERIFIED | Error/empty states | Static review & widget tests verify explicit loading, error, empty, and retry states on Books, History, Literature, Poetry, Canon, Oral Heritage, and detail routes. Failure resilience suite (`test/failure_resilience_test.dart`) exercises 404 routes, missing proverb/poet/work/history/book IDs, corrupted mastery JSON, and 200% font scaling. | Handled gracefully with fallback UI and localized empty states across all features. | Retain failure resilience test in CI. |
| LOC-001 | P1 | FIXED/VERIFIED | Persian parity & numeral formatting | Fixed 62 asymmetric localization keys in `lib/core/l10n/app_translations.dart`. Added complete Persian translations for settings, reading preferences, clear history dialog, about, and privacy policy. Zero-leak test passes with 23/23 assertions. | Asymmetric translation maps previously fell back or crashed. | Run `test/persian_zero_leak_test.dart` in CI to guarantee 100% key parity. |
| LOC-002 | P2 | FIXED/VERIFIED | Books provider label leaked Tajik text in Persian mode | The Book detail provider identity now uses a localized translation key: Tajik retains `Китобхон · kitobkhon.net`, while Persian uses the brand’s Latin form `Kitobkhon · kitobkhon.net`. The Persian Books presentation regression opens the detail screen, scrolls to the provider section, and verifies the localized label and RTL direction. The hardened browser audit now visits the Persian book detail route and requires the same provider marker in the rendered page. | The provider label was hardcoded in the detail widget instead of using the app translation map. | Keep provider/brand labels localized at the UI boundary and retain the widget plus browser regressions when catalog metadata changes. |
| LOC-003 | P2 | FIXED/VERIFIED | Corrupted generated Persian work titles | A data audit found two `needsReview` candidate titles whose generated Persian representations contained Latin extraction artifacts (`ACT` and `X`). Those representations are now withheld, the Tajik source titles remain auditable, and the Persian zero-leak regression rejects both Latin and Cyrillic characters in any remaining Persian work title. | Mechanical script conversion had no guard against non-Persian extraction artifacts. | Keep the zero-leak regression; withhold uncertain generated representations until the source title is manually verified instead of guessing a correction. |
| LOGIC-001 | P2 | FIXED/VERIFIED | Favorites persistence | Initialization is awaited before writes, and the saved-proverb runtime flow was exercised across navigation. | FavoritesNotifier now serializes initialization and writes. | Retain the race regression test and avoid introducing fire-and-forget preference writes. |
| REL-001 | P1 | PARTIAL | Android release signing | The current Gradle configuration fails closed when production secrets are missing. Fresh phone inspection confirms the installed `2.0.0+2004` debug APK is signed `CN=Android Debug`; no production keystore or trusted public certificate is available in this environment. Release verifiers now require protected `EXPECTED_RELEASE_CERT_SHA256` configuration and reject debug certificates. | Previous fallback and the historical hardcoded digest could have mistaken a debug identity for a production identity. | Configure the real production keystore and certificate digest in protected CI secrets; build and inspect the signed artifact before publishing. |
| REL-002 | P2 | PARTIAL | Web deployment/security | The current `gh-pages` deployment is `3020730` with cache `75792ceb8692e786ace2`; the live root/privacy URL both return HTTP 200. The live 8-viewport/209-route browser audit passes with no page errors, failed requests, console errors, or layout overflows. The release-preparation script now fails closed when the compiled shell lacks the `/zarbulmasal/` base path, preventing root-relative bootstrap publication. The hardened Playwright audit exits nonzero on those diagnostics and is pinned in the CI web job; CI cleanup asserts stale Android paths are absent when signing is unavailable; the workflow uses its scoped `GITHUB_TOKEN`; and both CI/manual Pages paths refresh `gh-pages` and use lease-protected pushes. | Publication is now verified for the web surface, but Android download publication remains intentionally withheld until production signing exists. | Retain CSP, offline-cache, base-path, stale-path, strict browser-diagnostic, idempotent-deploy, explicit-token, lease-protected push, and live privacy verification. |
| REL-003 | P1 | PARTIAL | Android artifact identity | The former ignored APKs inspected as versionName `1.1.0` with versionCodes 2003/3003/4003 and were moved to a recoverable quarantine; a checkout scan now finds no public-download APKs outside ignored build outputs. Current source is `2.0.0+2004`, but no production-signed current artifact has been verified. | Historical artifacts were present in the local checkout and were not products of the current source release. | Keep public-download staging fail-closed; build signed artifacts from the current commit and verify package, version, certificate, ABI, alignment, and checksums together before any publication. |
| REL-004 | P0 | PARTIAL | Production Android App Bundle Build | The current release pipeline builds a 59.7 MB QA-signed AAB with a temporary certificate and passes package/version, signature, native and official bundletool `PAGE_ALIGNMENT_16K`, mapping, native-symbol, Dart-symbol, and embedded-content evidence checks. Current QA AAB SHA-256 is `2b96592226b8824a2722f791c71c6a34e428e036d45495a835d820d21043be80` (59,676,653 bytes); the temporary QA certificate cannot close the production gate; CI still requires the protected production certificate digest and real keystore. | Production signing material is external to this worktree. | Run the secret-gated CI AAB build, verify its identity and Play Console acceptance, and retain the resulting artifact and mapping files. |
| REL-005 | P1 | FIXED/VERIFIED | Public privacy policy URL | The current web-only Pages deployment is `gh-pages` commit `3020730`; both the app root and `https://abubakrmmarufov-tech.github.io/zarbulmasal/privacy.html` return HTTP 200. The live 8-viewport/209-route browser audit completed with zero page errors, failed requests, console errors, or overflows. | Privacy information previously existed only as a short in-app paragraph, and the deployed Pages site was stale. | Keep the CI/manual post-deploy privacy verification and rerun the live browser sweep after each publication. |
| OPS-001 | P1 | FIXED/VERIFIED | Release rollback and recovery path | `docs/RELEASE_RUNBOOK.md` now defines the preflight gate, protected signing boundary, verified artifact separation, web/content rollback through reviewed `git revert`, Android staged-rollout halt and higher-version hotfix path, provenance recovery, and required incident evidence. | Release checks were documented across separate files without one recovery procedure for a high-impact regression. | Keep the runbook linked from README and update it when the release workflow or distribution surface changes. |
| CI-001 | P1 | FIXED/VERIFIED | Pull-request Android build | Release signing is intentionally fail-closed, so a PR job without secrets would fail if it attempted `flutter build apk --release`. | CI used the release build for unsigned PR validation. | PRs now build a debug APK without production signing secrets; only main publishes signed release APKs. |
| CI-002 | P1 | FIXED/VERIFIED | Coverage threshold enforcement | CI now parses the generated LCOV report and fails when aggregate line coverage drops below the documented 80% minimum; the current report is 82.19% (7,736/9,412 lines). The parser and pass/fail behavior are covered by `tool/test_check_coverage.py`. | The workflow generated coverage but did not enforce the project’s stated minimum. | Keep `python3 tool/check_coverage.py coverage/lcov.info --minimum 80` immediately after the Flutter coverage run. |
| DOC-001 | P1 | FIXED/VERIFIED | Audit/documentation drift | The register identifies the current branch and separates current evidence from the older QA audit. README, the current QA override, and the release report now agree on the 394-test suite, current coverage, privacy-policy artifact, quarantined literature candidates, portrait provenance, and unsigned-release limitation. The source register now explicitly distinguishes bibliographic leads from checked witnesses, including the consulted 2025 Grade 7 and Grade 11 editions. The dated sections in `docs/QA_AUDIT.md` remain explicitly historical and are not current release signoff. |
| PIPE-001 | P1 | FIXED/VERIFIED | Literature regeneration pipeline | `build_assets.py` dry-run reads extracted records and proposes candidates without writing; compatibility commands no longer reference deleted seed files. The active catalog remains the separately audited 159-record/5,501-work snapshot, with 145 public author records, 6 rejected extraction/non-author artifacts, and 8 unresolved reference records retained for review. | Production migration removed the old seed architecture without completing the pipeline migration. | Keep dry-run as the default, preserve curated records, use stable IDs for candidates, and run the asset validators before `--write`. |
| HIS-001 | P1 | FIXED/VERIFIED | History Grade 8 availability state | In a 320px local release build, select `Синфи 8` on History. The former generic empty search state was misleading because the Grade 8 source book exists but no chapter-level facts are verified. The screen now explains that exact limitation without a clipped reset control. | The filter treated a known source gap as an unsuccessful search. | Provider-override widget regression plus real browser flow; retain the honest unavailable copy until source details are verified. |
| HIS-002 | P2 | FIXED/VERIFIED | Events and oral narratives were not directly filterable | The source-bound History catalogue contains four event cards and two oral-narrative cards, but the visible category chips only named empires, people, and poems. Added localized `Рӯйдодҳо` / `Ривоятҳо` and `رویدادها` / `روایت‌ها` chips, without adding or changing historical claims. | The filter bar exposed only a subset of the implemented `HistoryEntryKind` values. | Failing-then-passing widget regression covers both Tajik filters and Persian labels. Fresh local release browser verified each filter selects only its expected card type, retains `#/history`, and does not leave the app. |
| HIS-003 | P1 | FIXED/VERIFIED | History claim provenance defaulting | Missing or unknown `claimProvenance.status` values now normalize to `NEEDS_REVIEW`; only explicit `VERIFIED_UPLOADED_BOOK_PAGE`, `SOURCE_LOCATED`, and `NEEDS_REVIEW` states survive both JSON parsing and direct model construction/serialization. | The model previously defaulted absent status to `VERIFIED_UPLOADED_BOOK_PAGE`, allowing malformed future records to look verified. | Covered by `test/history_domain_roundtrip_test.dart` and the history asset validation suite. |
| A11Y-001 | P1 | FIXED/VERIFIED | Minimum touch targets & Font Scaling | Shared text, script-switch, daily-hero, History action, History filters, Oral Heritage filters, and Books filter controls declare or retain a 48×48 minimum target. The Books filter rail previously constrained `FilterChip` semantics to about 38dp; its viewport now reserves 48dp and the widget regression measures every chip. The History and Oral Heritage rails now reserve the same 48dp target, with focused widget regressions. Native UIAutomator checks on Xiaomi Android 16 confirm visible clickable controls meet the 48dp bound, while the existing widget suite covers 100% and 200% font scale without fatal crash or clipped layout. | Several local overrides and constrained horizontal chip rails reduced Material controls below 48×48. | Enforced in Qalam/design screen layouts and verified by widget plus connected-device semantics audits. |
| A11Y-002 | P1 | FIXED/VERIFIED | Home/Explore search semantics | The Home and Explore search entries were tappable but exposed only labels and tap actions, without the `isButton` semantic flag. They now present localized search prompts as named buttons; `test/accessibility_test.dart` regresses both labels, tap actions, and button roles. | The entries used raw `GestureDetector` widgets without an explicit semantic contract. | Keep the semantics regression alongside the existing contrast and quiz-feedback checks. |
| A11Y-003 | P1 | FIXED/VERIFIED | Source facsimile semantics | The verified page-facsimile card in SourcePanel exposed inferred text and a tap action but no explicit button role. It now exposes one localized button label with the view/zoom instruction; `literature_presentation_test.dart` regresses the label, button role, and tap action. | The card relied on the child Text widgets and InkWell’s default semantics. | Keep the source-panel semantics regression for every localized page-image action. |
| A11Y-004 | P1 | FIXED/VERIFIED | Literary Heritage card contrast | The light-theme Literary Heritage card rendered its eyebrow and supporting copy on an ink surface at measured contrast ratios of 3.46:1 and 3.52:1, below the 4.5:1 normal-text threshold. The palette now uses `antiqueGold` and the paper-text ramp; `accessibility_test.dart` measures both light and dark card text. | The card reused soft burgundy and muted ink colors designed for light surfaces while its background remained dark. | Keep the widget-level measured contrast regression whenever the editorial card palette changes. |
| A11Y-005 | P1 | FIXED/VERIFIED | Daily Verse card contrast | The light-theme Daily Verse card reused `burgundySoft` and `inkMute` on an ink surface at measured ratios of 3.46:1 and 3.52:1. Its eyebrow, author, and supporting copy now use the accessible gold/paper-text ramp; `literature_presentation_test.dart` measures the real rendered labels against the card surface. | The card shared soft light-surface accents even though its background is dark in both themes. | Keep the widget-level measured contrast regression whenever the Daily Verse palette changes. |
| A11Y-006 | P1 | FIXED/VERIFIED | Source review-status badge contrast | The light SourcePanel “under review” badge rendered its normal-sized label at 3.53:1 against its tinted badge background. The status now uses theme-aware `burgundy` on light surfaces and `antiqueGoldSoft` on dark surfaces; `literature_presentation_test.dart` measures the foreground against the actual 12%-tint background. | The same soft status color was used as both normal text and a translucent background tint across themes. | Keep semantic status colors contrast-tested in both light and dark SourcePanel states. |
| A11Y-007 | P1 | FIXED/VERIFIED | Portrait semantics duplication | Source-backed poet portraits now expose one citation-aware image node instead of repeating the author label through both the portrait wrapper and the nested image semantics. The regression verifies the citation label and that the nested image resolves to the same semantics node. | The portrait widget supplied an inner image label while its outer semantics node already carried the accessible citation and fallback state. | Keep the single-node semantics regression with portrait and Persian/RTL layout coverage; native TalkBack verification remains a separate release gate. |
| LIT-004 | P1 | FIXED/VERIFIED | Literature search hygiene | Search `Unknown` and the Persian keyboard spelling `رودكي`. Unresolved importer placeholders no longer appear, while common Arabic/Persian `ي/ك`, marks, and zero-width variants match canonical Persian names. | Search surfaces used raw lowercase text and global search did not exclude noncanonical author records. | Unit test for normalization; widget and local browser tests for placeholder suppression and Rudaki search. |
| NAV-001 | P1 | FIXED/VERIFIED | Shareable web deep links | A fresh `#/history` browser entry now opens History; internal navigation updates the fragment (for example `#/literature`) and browser Back returns to Home. | The app forced `initialLocation: '/'`, overriding hash deep links, and GoRouter 14 does not reflect imperative navigation unless configured. | Router configuration regression test plus clean-origin browser verification. Document fragment-style URLs in release notes. |
| PERF-001 | P1 | FIXED/VERIFIED | QuizEngine word tokenization burn | Quiz generation executed 5.4 million redundant regex operations and Set allocations across 2,000 quizzes (18.0s). Static bounded word set cache (`_wordCache`) reduced execution time to 519ms (34.7x speedup, 0.25ms/quiz). | `wordSimilarity` re-tokenized correct meaning and distractors on every candidate evaluation. | Tested via `tool/benchmark_quiz.dart` and `test/quiz_algorithm_test.dart` (11/11 tests pass with 2,000 collision simulations). |
| PERF-002 | P2 | FIXED/VERIFIED | Literary catalog load amplification | The 9 MB `works.json` catalog is now decoded with Flutter’s background computation helper where supported and memoized after a successful repository load, preventing repeated allocation/decoding during author, search, and detail lookups. Failed futures are evicted so retry remains possible. | The repository reparsed the largest bundled catalog on every direct `loadWorks()` call. | Covered by `test/features/literature/data/literature_repository_test.dart`; the regression verifies one successful asset load and two attempts after failure. |
| L10N-001 | P1 | FIXED/VERIFIED | Home Persian numeral consistency | Verified Persian numeral formatting across all routes. Home, Literature, History, and Settings format digits using Persian script when display language is Persian. | Fixed in Qalam design system and AppTranslations. | Retain Persian zero leak test. |
| L10N-002 | P1 | FIXED/VERIFIED | Quiz Cyrillic text direction isolation in Persian mode | The review card wraps Tajik Cyrillic explanation and example text in `Directionality(textDirection: TextDirection.ltr)`; `learning_mastery_test.dart` asserts the LTR boundary in Persian mode. | Directionality isolation prevents punctuation flip. | Verified in `learning_mastery_test.dart`. |
| LRN-001 | P1 | FIXED/VERIFIED | Flashcards & Quiz spaced mastery persistence | `ProverbMasteryNotifier` persists review state to `SharedPreferences`; Flashcards and Quiz both call `recordReview`. Unit coverage checks serialization, reload, and persistence. | State is correctly read and stored. | Verified in `learning_mastery_test.dart`. |
| LRN-002 | P2 | FIXED/VERIFIED | Flashcard mastery filtering | Flashcards exposes all/again/learning/mastered chips with live counts and an empty-filter state; the learning-mastery widget tests cover both. | Consumes active filter with reactive counts. | Verified in `learning_mastery_test.dart`. |
| LRN-003 | P2 | FIXED/VERIFIED | Quiz results reinforcement flow | Quiz results expose “Practice missed” only when misses exist. The action sets `flashcardsFilterProvider` to `again`; Flashcards now consumes that shared filter on initialization and shows only missed cards. | Flashcards previously initialized its private filter to `all`, silently ignoring the filter Quiz had selected. | The provider-backed widget regression seeds one missed proverb and requires the selected `Again (1)` chip and that proverb’s flashcard. |
| AUD-001 | P2 | FIXED/VERIFIED | Playwright audit localStorage JSON serialization | Playwright audit scripts set unquoted strings (`'fa'`) in localStorage, causing `shared_preferences_web` JSON decoding to throw FormatException and fall back to Tajik. | `shared_preferences_web` 2.4.3 uses `json.decode(encodedValue)` requiring quoted JSON strings. | Use `JSON.stringify(...)` in all browser audit init scripts; verified with Playwright Chromium. |
| AUD-002 | P1 | FIXED/VERIFIED | Persian browser audit false positive | The prior Persian route pass wrote Flutter Web preferences as raw strings and inspected CanvasKit body text before enabling Flutter semantics, so it could report a pass while the visible app remained in Tajik mode. The harness now JSON-encodes preferences, activates `flt-semantics-placeholder`, requires localized markers on seven Persian route visits, rejects the Tajik home marker `Асосӣ`, and the latest local release sweep shows Persian RTL UI in the saved screenshots with zero browser diagnostics. | The audit asserted route completion and diagnostics but did not verify the persisted preference format or localized rendered content. | Keep the JSON-storage regression and localized marker assertions in the Python audit suite; retain visual Persian/RTL screenshots in each release sweep. |
| SET-001 | P1 | FIXED/VERIFIED | Settings & Reader Preferences Overhaul | Rebuilt `SettingsScreen` with Appearance, Reader Typography (font size delta, line spacing, standard/parallel reader mode), Language, Data (clear history dialog with confirmation, onboarding replay), Legal & Info (proverbs count, source disclosure dialog, privacy policy dialog, open-source licenses `showLicensePage`, contact, version). | Reader preferences were decoupled and unpersisted. | Covered in `test/features/settings/settings_screen_test.dart` (6/6 pass). |
| SEA-001 | P1 | FIXED/VERIFIED | Tiered Search Relevance & Ranking | Implemented `scoreMatch` and `scoreMatchAny` in `SearchNormalizer` (exact 100, normalized exact 90, prefix 75, normalized prefix 65, word prefix 50, partial 35, transliterated 20). Sorted all Global Search results by relevance descending; retained raw user query in empty state. | Search previously returned arbitrary catalog order without relevance weighting. | Covered in `test/features/search/global_search_test.dart` and `test/search_normalizer_test.dart`. |
| RES-001 | P1 | FIXED/VERIFIED | Model & Runtime Failure Resilience | Hardened `ProverbMastery.fromJson` to handle non-numeric review/correct counts without throwing; hardened `RecentActivity` persistence to discard malformed records individually and reject external routes; hardened `BookEdition` URI getters to reject JavaScript/HTTP/file schemes; and hardened `QuizEngine` against empty or non-traditional catalogs. | Corrupted storage or malformed payloads could cause unhandled exceptions or erase otherwise valid recent history. | Covered in `test/failure_resilience_test.dart` (12/12 pass). |
| SEC-001 | P1 | FIXED/VERIFIED | External URL trust boundary | Book, provider-catalogue, history, cover, download, and source links now accept only HTTPS URLs on the approved `kitobkhon.net` / `maorif.tj` host families; userinfo, explicit non-443 ports, and untrusted hosts are rejected. Provider catalogue metadata is validated before it enters the runtime, and the shared trusted launcher revalidates every platform-bound URI at the sink. | HTTPS-only checks did not prevent phishing or unexpected third-party navigation, and provider catalogue metadata had no repository validation boundary. | Covered by `test/trusted_url_policy_test.dart`, book/history/provider-catalogue URL regressions, `test/trusted_url_launcher_test.dart`, and `test/features/history/history_source_launcher_test.dart`. |
| SEC-002 | P1 | FIXED/VERIFIED | Persisted reader preferences | Non-finite, out-of-range, and unknown persisted line-height/mode values are rejected and setter inputs are sanitized to the bounded supported domain. | Preference state was parsed without complete bounds or mode whitelisting. | Covered by `test/features/literature/data/reader_preferences_test.dart`; full suite 396/396 passes. |
| SEC-003 | P1 | FIXED/VERIFIED | Literature source-path containment | Textbook extraction now resolves source paths beneath `docs/literature/pdfs`, rejects traversal/absolute/NUL/non-PDF/oversized inputs, and handles unsafe references as unavailable candidates. | Candidate extraction accepted paths outside the approved PDF corpus. | Covered by `tool/literature_pipeline/scripts/test_index_textbook_pages.py` (8/8 pass). |
| SEC-004 | P1 | PARTIAL | Release artifact and build supply chain | Gradle wrapper distribution checksum and dependency verification metadata now include the AAPT2 artifacts required by the Android build; CI uses `flutter pub get --enforce-lockfile`, strict Gradle dependency verification, and a plain offline `assembleDebug`. The repository verifier checks native ELF PT_LOAD alignment for 16 KB-page compatibility, the pinned official bundletool 1.18.3 verifier requires `PAGE_ALIGNMENT_16K`, `verify_android_release_artifacts.sh` requires non-empty mapping, native-symbol, and Dart-symbol evidence before archival, and the CI signing preflight rejects a missing, malformed, non-private-key, or wrong-certificate keystore before release compilation. The bundle verifier now compares certificate digests case-insensitively. A current temporary-QA-signed AAB passes structural/alignment/bundletool/identity/evidence checks, but no signed Play AAB has been produced here. | Release identity and supply-chain checks were incomplete while production secrets remained external; the prior multi-path artifact could also have changed the APK archive root. | Keep the locked pub resolution, strict dependency verification, CI secret gate, exact artifact identity checks, separate APK/evidence artifact roots, retained deobfuscation/symbol evidence, native and bundletool 16 KB checks, and real-certificate verification as release blockers. |
| SEC-005 | P1 | PARTIAL | Standard security scan coverage | The latest sealed Codex Security Standard scan (`1cbcd37a-d3ad-4874-b6a1-261499b52de9`) reported 0 reportable findings across the inspected Flutter runtime/state, external URL, Android release, web/cache, GitHub publication, provenance, and dependency boundaries. The 761-file worktree inventory was available, but coverage is explicitly partial because protected GitHub controls, production signing, live Pages publication, physical-device upgrade, and a fresh advisory database were not observable. Finalization also skipped seven malformed receipt references; no finding records were lost, but the scan remains a bounded source review rather than a repository-wide clean sign-off. Earlier sealed Standard/diff scans also reported 0 findings in their narrower scopes. | A complete production security sign-off cannot be claimed from static source review while external release controls and runtime evidence remain unavailable. | Preserve the sealed scan artifacts and keep the signed-artifact, device, live-host, protected-secret, and dependency-advisory gates open until directly verified. |
| SEC-006 | P2 | FIXED/VERIFIED | Android package visibility minimization | The release manifest no longer queries `http` VIEW handlers; it retains only HTTPS and Flutter text-processing queries, matching the runtime’s trusted HTTPS URL policy. `test/android_distribution_test.dart` guards the native manifest contract. | A stale template query advertised HTTP handlers after the app had become HTTPS-only. | Keep package-visibility queries limited to schemes and intents the shipped runtime can actually use. |
| SEC-007 | P1 | FIXED/VERIFIED | Unhandled Android custom-scheme entry point | The release manifest no longer advertises `zarbulmasal://`; the app has no custom-scheme parser or privileged deep-link route. `test/android_distribution_test.dart` now fails if the scheme is reintroduced, and the strict offline Android debug build passes after removal. | The manifest exposed a browsable custom scheme without a corresponding runtime handler. | Keep Android intent filters limited to implemented entry points and require a regression test for every exposed scheme. |
| PHONE-001 | P2 | FIXED/VERIFIED | Settings mixed-script leak in Cyrillic subtitle | Live phone inspection of Settings revealed `subtitle: isPersian ? 'خط فارسی' : 'Хатти форсӣ'` with an erroneous Persian `س` (U+0633) inside Cyrillic text. Replaced with localized `tr('settings_script_persian')`. | Hardcoded string literal contained an accidental Arabic/Persian letter. | Verified on Xiaomi 2412DPC0AG (POCO F7, Android 16); zero Persian characters in Cyrillic mode. |
| PHONE-002 | P2 | FIXED/VERIFIED | Settings missing translation key display | Replacing script subtitle with `tr('settings_script_tajik')` caused raw key text `settings_script_tajik` to render because the translation key in `AppTranslations` is `settings_script_cyrillic`. Replaced with `tr('settings_script_cyrillic')`. Audited all 673 translation keys in `lib/` (0 missing). | Key name mismatch between UI and translation dictionaries. | Rebuilt and reinstalled release APK on phone; verified `Хатти кириллӣ` in Tajik mode and `خط سیریلیک` in Persian mode. |
| PHONE-003 | P1 | PARTIAL | Real-Phone Runtime Hardware QA | Fresh Xiaomi `2412DPC0AG` / Android 16 native QA now passes on the current debug build: app launch, Literature hub, poet list with portraits/placeholders, works gate, Literature search with keyboard, Books, Persian RTL, Tajik switch, dark mode, and light-mode restoration; no crash, fatal exception, or ANR lines were observed. A signed-release upgrade and the full historical flow matrix remain unproven. | The production signing artifact is still unavailable; connected-device runtime evidence is debug-only. | Run the complete smoke/upgrade matrix against the secret-gated signed AAB/APKs, including data preservation, offline mode, Persian/RTL, typography, and logcat. |
| SEC-008 | P1 | PARTIAL | Portrait redistribution rights | 67 authentic local portraits are source-backed and rendered by `QalamPortrait`, but their records explicitly carry `rightsStatus: unknown`; locating a portrait in an uploaded textbook or `maorif.tj` is not proof of redistribution permission. | The literature requirement prioritizes authentic source portraits, while rights clearance is external to the source citation. | Obtain permission/public-domain evidence for each portrait before public distribution, or switch uncleared records to the existing consistent placeholder. |
| SEC-009 | P1 | PARTIAL | Audit page images in public repository | Twelve textbook page-image binaries remain Git-tracked as provenance evidence. They are excluded from the Flutter/web asset bundle, but a public GitHub repository still distributes them and retains repository history. | Local visual proof and public source distribution currently share the same checkout. | Move page proofs to a private evidence store or establish redistribution rights; preserve only hashes/citations in the public repository, with history rewrite requiring explicit owner approval. |
| SEC-010 | P2 | PARTIAL | External-link redirect boundary | The policy now allows only the exact `kitobkhon.net`, `maorif.tj`, and explicit `cdn.kitobkhon.net` hosts; arbitrary subdomains are rejected and the focused URL/Books suite passes. `url_launcher` still hands navigation to the external browser, so redirects/content served by an allowed host remain outside app control. | External source links are intentionally opened outside the app. | Keep the exact-host regression, warn users before external navigation, and treat host compromise/redirects as an external-service risk rather than app trust. |
| SEC-011 | P2 | FIXED/VERIFIED | Android local-state backup contract | Android `allowBackup` remains enabled by deliberate product choice for local preferences/history/learning state. The in-app and public privacy policies disclose that the OS/account may back up or transfer this data, and `test/android_distribution_test.dart` asserts the manifest contract. | Backup behavior is a product choice; the app does not access or control OS/cloud retention. | Revisit only if the product owner chooses privacy-first opt-out; otherwise retain the disclosure and regression test. |
